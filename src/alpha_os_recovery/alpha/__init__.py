"""Retired compatibility shell.

In the current model, predictive units are hypotheses and alpha is reserved
for excess return as an outcome. New runtime and research code should depend
on `hypotheses`, `research`, or other current packages instead of `alpha`.
No `alpha` submodules remain in the repo.
"""
