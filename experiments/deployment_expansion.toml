[defaults]
asset = "BTC"
start_date = "2025-09-01"
end_date = "2026-03-05"
registry_mode = "admission"
admission_source = "candidates"
deployment_mode = "refresh"
sizing_mode = "runtime"
notes = "Deployment size expansion sweep after the 40-60 comparison."

[defaults.overrides]
lifecycle.candidate_quality_min = 1.10
lifecycle.active_quality_min = 0.0
paper.rebalance_deadband_usd = 5.0
deployment.max_replacements = 1
deployment.promotion_margin = 0.05

[[experiment]]
name = "deployment-60"

[experiment.overrides.deployment]
max_alphas = 60

[[experiment]]
name = "deployment-70"

[experiment.overrides.deployment]
max_alphas = 70

[[experiment]]
name = "deployment-80"

[experiment.overrides.deployment]
max_alphas = 80

[[experiment]]
name = "deployment-90"

[experiment.overrides.deployment]
max_alphas = 90
