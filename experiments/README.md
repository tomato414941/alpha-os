# Experiments

This directory holds human-readable investment hypotheses and any later
artifacts needed to evaluate them.

Start with one markdown file per hypothesis under `hypotheses/`. Create a
per-hypothesis directory only when that hypothesis has multiple artifacts.
